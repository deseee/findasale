name: Scrape Nebraska Auctioneer Licenses

on:
  schedule:
    - cron: '30 8 * * 3'   # Wednesday 8:30am UTC
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Nebraska Licensing Scraper
        env:
          OUTREACH_SECRET: ${{ secrets.INTERNAL_API_TOKEN }}
          RAILWAY_BACKEND_URL: ${{ secrets.RAILWAY_BACKEND_URL }}
        run: |
          curl -X POST \
            -H "x-internal-secret: ${OUTREACH_SECRET}" \
            "${RAILWAY_BACKEND_URL}/api/internal/scraper/run-nebraska-licensing" \
            --fail-with-body \
            --connect-timeout 10 \
            --max-time 3600

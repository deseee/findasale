name: Scrape Oregon Pawnbroker Licenses (Phase 2)

on:
  schedule:
    # Monday 11:45 AM UTC
    - cron: '45 11 * * 1'
  workflow_dispatch:

jobs:
  scrape:
    name: Oregon Phase 2 Pawnbroker Scraper
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install pnpm
        uses: pnpm/action-setup@v2

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Oregon Phase 2 Scraper
        working-directory: packages/backend
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          npx tsx -e '
            import { runOregonPhase2Scraper } from "./src/services/scraper/sources/oregonPhase2Scraper";
            try {
              await runOregonPhase2Scraper();
              console.log("Oregon Phase 2 scraper completed successfully");
              process.exit(0);
            } catch (err) {
              console.error("Oregon Phase 2 scraper failed:", err);
              process.exit(1);
            }
          '

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: 1,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '⚠️ Oregon Phase 2 pawnbroker scraper failed. Check workflow logs for details.'
            })

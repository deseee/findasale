name: Scrape New Mexico Pawnbroker Licenses (Phase 2)

on:
  schedule:
    # Monday 10:45 UTC
    - cron: '45 10 * * 1'
  workflow_dispatch:

jobs:
  scrape:
    name: New Mexico Phase 2 Pawnbroker Scraper
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'


      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install --frozen-lockfile


      - name: Generate Prisma client
        run: npx prisma generate
        working-directory: packages/database
      - name: Run New Mexico Phase 2 Scraper
        working-directory: packages/backend
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          npx tsx -e '
            import { runNewMexicoPhase2Scraper } from "./src/services/scraper/sources/newmexicoPhase2Scraper";
            (async () => { try {
              await runNewMexicoPhase2Scraper();
              console.log("New Mexico Phase 2 scraper completed successfully");
              process.exit(0);
            } catch (err) {
              console.error("New Mexico Phase 2 scraper failed:", err);
              process.exit(1);
            }
            })();
          '
name: Scrape Pennsylvania Business Registry (Phase 2)

on:
  schedule:
    # Tuesday 8:00 AM UTC
    - cron: '0 8 * * 2'
  workflow_dispatch:

jobs:
  scrape:
    name: Pennsylvania Phase 2 Business Registry Scraper
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install pnpm
        uses: pnpm/action-setup@v2

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Pennsylvania Phase 2 Scraper
        working-directory: packages/backend
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          npx tsx -e '
            import { runPennsylvaniaPhase2Scraper } from "./src/services/scraper/sources/pennsylvaniaPhase2Scraper";
            try {
              await runPennsylvaniaPhase2Scraper();
              console.log("Pennsylvania Phase 2 scraper completed successfully");
              process.exit(0);
            } catch (err) {
              console.error("Pennsylvania Phase 2 scraper failed:", err);
              process.exit(1);
            }
          '

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: 1,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '⚠️ Pennsylvania Phase 2 business registry scraper failed. Check workflow logs: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'
            })

name: Scrape Montana Pawnbroker Licenses (Phase 2)

on:
  schedule:
    # Monday 09:00 UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:

jobs:
  scrape:
    name: Montana Phase 2 Pawnbroker Scraper
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'


      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install --frozen-lockfile


      - name: Generate Prisma client
        run: npx prisma generate
        working-directory: packages/database
      - name: Run Montana Phase 2 Scraper
        working-directory: packages/backend
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          npx tsx -e '
            import { runMontanaPhase2Scraper } from "./src/services/scraper/sources/montanaPhase2Scraper";
            (async () => { try {
              await runMontanaPhase2Scraper();
              console.log("Montana Phase 2 scraper completed successfully");
              process.exit(0);
            } catch (err) {
              console.error("Montana Phase 2 scraper failed:", err);
              process.exit(1);
            }
            })();
          '
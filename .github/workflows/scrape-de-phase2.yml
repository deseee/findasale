name: Scrape Delaware Pawnbroker Licenses (Phase 2)

on:
  schedule:
    # Monday 7:15 AM UTC
    - cron: '15 7 * * 1'
  workflow_dispatch:

jobs:
  scrape:
    name: Delaware Phase 2 Pawnbroker Scraper
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'


      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install --frozen-lockfile


      - name: Generate Prisma client
        run: npx prisma generate
        working-directory: packages/database
      - name: Run Delaware Phase 2 Scraper
        working-directory: packages/backend
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
        run: |
          npx tsx -e '
            import { runDelawarePhase2Scraper } from "./src/services/scraper/sources/delawarePhase2Scraper";
            (async () => { try {
              await runDelawarePhase2Scraper();
              console.log("Delaware Phase 2 scraper completed successfully");
              process.exit(0);
            } catch (err) {
              console.error("Delaware Phase 2 scraper failed:", err);
              process.exit(1);
            }
            })();
          '
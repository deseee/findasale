# Health Report — 2026-05-07

**Session:** S680  
**Scope:** Full monorepo (packages/backend, packages/frontend)  
**Scans run:** 7 (Secrets, Auth/CORS, Code Quality, SSR Safety, Prisma, UI/A11y, Env Config)

---

## Summary

Overall posture is **solid for a pre-beta product**. Auth, CORS, JWT, file upload security, and rate limiting are all correctly implemented — no auth bypass paths found. The critical operational concern is the `.env` file living in the repo directory (confirmed in `.gitignore` but git history must be audited). SSR hydration risk in `calendar.tsx` and unconfirmed Leaflet `ssr:false` wrapping are the highest-priority code fixes. Accessibility has widespread gaps (20+ missing alt texts, 20+ missing ARIA labels) but these are captured by the planned #391 WCAG audit. Admin-only `findMany` unboundedness is low real-world risk (auth-gated) but should be swept as tech debt.

**Top 3 concerns:** (1) `.env` git history audit, (2) `calendar.tsx` SSR hydration crash, (3) Leaflet components without confirmed `ssr:false`.

---

## Critical (blocker — fix before next deploy)

- **`.env` in repo directory with live secrets** — `packages/backend/.env` — File contains JWT_SECRET, STRIPE_SECRET_KEY, RESEND_API_KEY, VAPID keys, Cloudinary + Twilio credentials. `.gitignore` covers it but git history must be audited. Run: `git log --all -- packages/backend/.env` — if any commits appear, secrets must be rotated immediately. **Patrick action required.**

---

## High (fix this week)

- ~~**SSR hydration crash** — `calendar.tsx`~~ ✓ FALSE POSITIVE — window.innerWidth already wrapped in useEffect. Verified S680.
- ~~**Leaflet SSR safety unconfirmed**~~ ✓ FALSE POSITIVE — All Leaflet components correctly wrapped via `SaleMap.tsx` and `EntrancePinPicker.tsx` dynamic wrappers. Verified S680.
- **`.env.example` drift** — 12 variables out of sync. Missing from `.env.example`: `MAILERLITE_API_TOKEN`, `STRIPE_BILLING_WEBHOOK_SECRET`, `STRIPE_CONNECT_WEBHOOK_SECRET`, `STRIPE_GENERIC_ITEM_PRODUCT_ID`, `STRIPE_HUNT_PASS_PRICE_ID`. Missing from `.env`: `AI_COST_CEILING_USD`, `GOOGLE_PLACES_API_KEY`, `EBAY_DELETION_ENDPOINT_URL`, `EBAY_VERIFICATION_TOKEN`, `OLLAMA_URL`, `OSRM_API_URL`, `RATE_LIMIT_WHITELIST_IPS`. New devs (or a new Railway env) will misconfigure silently.

---

## Medium (fix this sprint)

- **Production `alert()` calls** — `hubs/create.tsx` (3), `hubs/manage.tsx` (2), `UGCPhotoSubmitButton.tsx` (3), `inventory.tsx` (1), `xp-velocity.tsx` (1) — Blocks workflow, hostile on mobile. Replace with toast/inline error.
- **Production `console.log` in pages** — `stripe-connect.tsx:44` (`console.log('Onboarding status updated:', data)`) — Leaks internal state to browser console. Remove or wrap in `process.env.NODE_ENV === 'development'` guard.
- **29 TODO/FIXME markers** — Scattered across `bountyController.ts`, `citiesController.ts`, `auctionJob.ts`, `deliverabilityMonitorJob.ts` — Distance sorting, Stripe checkout URL placeholders, Phase 2 deferrals. Not bugs but signal unfinished work near payment/auction logic.
- **Unbounded `findMany` in admin controllers** — `adminBroadcastController.ts` (5), `adminController.ts` (14+), `adminReportsController.ts` — No `take`/`skip` pagination. Risk is low (admin-only, auth-gated) but should be capped to prevent accidental memory exhaustion.
- **Broad `include: true` Prisma queries** — `authController.ts:166`, `billingController.ts:40`, `bountyController.ts` (6), `buyingPoolController.ts:24`, `checklistController.ts:131+299` — Full relation objects returned. Use `select` to whitelist only needed fields, especially where responses are serialized to API clients.

---

## Low (track, fix when relevant)

- **20+ `<img>` tags missing `alt`** — `PreviewModal.tsx`, `CartDrawer.tsx`, `ItemCard.tsx`, `RapidCapture.tsx`, etc. — WCAG 2.1 violation. Captured by planned #391 WCAG audit — do not dispatch separately.
- **20+ buttons/inputs missing `aria-label`** — `AvatarDropdown.tsx`, `ACHPayoutButton.tsx`, `ActionBar.tsx`, etc. — WCAG violation. Same: captured by #391.
- **Multiple `.env` load paths** — `packages/backend/src/index.ts` attempts 5 different `.env` paths. Defensive but could mask misconfiguration. Standardize to one path with a clear error if missing.

---

## Clean Checks

- **Scan 1A — Hardcoded secrets in source:** No hits ✓
- **Scan 1B — Sensitive console.log (passwords/tokens):** No hits ✓
- **Scan 2A — Auth middleware coverage:** All mutating routes correctly gated ✓
- **Scan 2B — CORS:** Whitelist-only, no wildcard, credentials correctly set ✓
- **Scan 2C — JWT verification bypass:** No bypass patterns found ✓
- **Scan 7B — Placeholder values in .env:** None found ✓

---

## Self-Healing Reference

| Finding | Pattern in self_healing_skills.md? | Action |
|---------|-----------------------------------|--------|
| SSR browser globals | Likely documented — check `self_healing_skills.md` | findasale-dev |
| Leaflet ssr:false | Common Next.js pattern | findasale-dev |
| alert() → toast | Recurring pattern | findasale-records: add as Skill |
| Unbounded findMany | Not documented | findasale-records: add as Skill |
| Broad include: true | Not documented | findasale-records: add as Skill |

---

## Self-Healing Candidates

- **`alert()` → toast replacement** — ✓ Fixed S680 (6 files). Add to `self_healing_skills.md` as Skill: "Replace `alert()` with `showToast()` from ToastContext."
- **Unbounded `findMany` pagination** — Recurring in admin controllers. Add as Skill: "Add `take: 100, skip: offset` to any admin `findMany` without pagination. Wire `?page=` query param."
- **Broad `include: true` → `select`** — Appears on every controller that returns relational data. Add as Skill: "Audit controller returns — replace `include: { relation: true }` with `select: { id, field1, field2 }` for public-facing endpoints."

---

## Recommended Next Actions

1. **Patrick (now):** Run `git log --all -- packages/backend/.env` — if any output, rotate all credentials in Railway immediately.
2. **findasale-dev dispatch:** Fix `calendar.tsx` SSR hydration (window.innerWidth → useEffect) + verify/fix Leaflet parent pages use `dynamic({ ssr: false })`.
3. **findasale-dev dispatch:** Replace 10 `alert()` calls with toasts across hubs/UGC/inventory pages + remove `stripe-connect.tsx` console.log.
4. **findasale-ops dispatch:** Sync `.env.example` — add 5 missing vars, remove 7 dead vars.
5. **#391 WCAG audit (next session):** Alt text + ARIA label sweep is covered by the planned accessibility audit — do not duplicate dispatch now.
6. **findasale-records:** Add 3 new patterns to `self_healing_skills.md` (alert→toast, unbounded findMany, broad include:true).

---

## Routing Summary

- **findasale-dev:** 2 dispatches — (A) SSR/Leaflet hydration fixes, (B) alert()→toast + console.log removal
- **findasale-ops:** 1 dispatch — .env.example sync
- **findasale-records:** 1 dispatch — 3 new self-healing patterns
- **Patrick direct:** git history audit for .env (1 command)
- **Deferred to #391:** alt text (20+), ARIA labels (20+) — captured by WCAG audit
- **No action needed:** Auth, CORS, JWT, file upload, rate limiting, SQL injection (all clean)

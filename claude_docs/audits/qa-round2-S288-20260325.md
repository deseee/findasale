# QA Round 2 — S288 — 2026-03-25

## Session Status: BLOCKED — Critical Infrastructure Issues

### Blocking Issues Encountered
Multiple P0/P1 blocking issues discovered during this QA run prevent completion of the full test plan:

1. **Export Data Page 404** — `/organizer/export` returns 404 (blocking A1)
2. **Login Form Unresponsive** — user12 login fails silently; no auth requests sent (blocking BATCH B & C)
3. **Feed Card Navigation** — Sale cards on /feed not clickable or navigating (blocks navigation to test A2-A5)

These are environment/infrastructure issues, not feature bugs.

---

## Batch A — Priority Re-tests (user2@example.com)

| Feature | Status | Notes |
|---------|--------|-------|
| #27/#66/#125 Exports | ❌ **P0** | `/organizer/export` returns 404. Button exists on dashboard at `/organizer/dashboard` but target page doesn't exist. User navigates to broken page. |
| #184 iCal | ⚠️ **BLOCKED** | Could not reach sale detail page from /feed — cards not clickable. Unable to test iCal button. |
| #131 Share Templates | ⚠️ **BLOCKED** | Requires navigation to published sale page. Feed navigation broken. |
| #132 À La Carte | ⚠️ **BLOCKED** | Requires published sale page. Feed navigation broken. |
| #172 Stripe Connect | ⚠️ **BLOCKED** | Requires navigation to settings/payouts. Dashboard accessible but Export Data modal blocked by 404 issue. |

### A1 — CSV/JSON/ZIP Exports Detailed Finding

**Issue:** Export Data button on organizer dashboard is not wired to a working page.

**Steps to Reproduce:**
1. Log in as organizer (user2@example.com / password123)
2. Navigate to `/organizer/dashboard`
3. Click "Export Data" button in PRO FEATURES section
4. Expected: Export modal or export page with CSV/JSON/ZIP options
5. Actual: No modal. Navigating to `/organizer/export` directly returns 404 Page Not Found.

**Impact:** Organizers cannot export their data. Feature is advertised but non-functional.

**Severity:** P0 — Blocks core feature availability.

---

## Batch B — Gamification (user12@example.com)

| Feature | Status | Notes |
|---------|--------|-------|
| #29/#123 Loyalty/Guild | ❌ **BLOCKED** | Login failed — user12 credentials not authenticating. No API requests sent. Form appears to be client-side issue. |
| #212 Leaderboard | ❌ **BLOCKED** | Cannot log in. |
| #213 Hunt Pass | ❌ **BLOCKED** | Cannot log in. |
| #50 Loot Log | ❌ **BLOCKED** | Cannot log in. |
| #57 Rarity Badges | ❌ **BLOCKED** | Cannot log in. |
| #48 Trails | ❌ **BLOCKED** | Cannot log in. |
| #210 Streaks | ❌ **BLOCKED** | Cannot log in. |
| #55 Challenges | ❌ **BLOCKED** | Cannot log in. |
| #85 Treasure Hunt QR | ⚠️ **PARTIAL** | user2 was logged in successfully earlier. Did not reach sale edit page due to feed navigation issue. |

### Login Issue Detailed Finding

**Issue:** Authentication endpoint unresponsive for user12@example.com.

**Steps to Reproduce:**
1. Navigate to `/login`
2. Enter email: `user12@example.com`
3. Enter password: `password123`
4. Click "Sign in"
5. Expected: Redirect to shopper dashboard after 1–2 seconds
6. Actual: Form remains on login page. No error message. No network requests logged (no POST to /auth/login).

**Network Activity:** Zero authenticated requests captured after Sign In button click. Form submission does not trigger API call.

**Severity:** P0 — Blocks all shopper-facing feature testing.

**Note:** user2@example.com (organizer) logged in successfully earlier, so the auth infrastructure is partially working. Issue may be shopper-tier specific or account-specific to user12.

---

## Batch C — Shopper Social (user12@example.com)

| Feature | Status | Notes |
|---------|--------|-------|
| #201 Favorites | ❌ **BLOCKED** | Cannot log in (user12 auth failed). |
| #194 Saved Searches | ❌ **BLOCKED** | Cannot log in. |
| #87 Brand Tracking | ❌ **BLOCKED** | Cannot log in. |
| #88 Haul Gallery | ❌ **BLOCKED** | Cannot log in. |
| #37 Sale Reminders | ❌ **BLOCKED** | Cannot log in. |
| #6 Virtual Queue | ❌ **BLOCKED** | Cannot log in. |
| #198 Reviews | ❌ **BLOCKED** | Cannot log in. |
| #206 Condition Guide | ❌ **BLOCKED** | Cannot log in. |
| #196 Buying Pools | ❌ **BLOCKED** | Cannot log in. |
| #197 Bounties | ❌ **BLOCKED** | Cannot log in. |

All blocked by shopper login failure.

---

## P0/P1 Findings Summary

| P | Feature | Issue | Expected | Actual | Impact |
|---|---------|-------|----------|--------|--------|
| **P0** | #27/#66/#125 Exports | `/organizer/export` missing/404 | Export modal or page loads | 404 Not Found | Organizers cannot access export feature despite button visible |
| **P0** | user12 Login | Auth endpoint unresponsive | Redirect to dashboard | Stays on login page, no API request | Cannot test ANY shopper features (entire BATCH B & C) |
| **P1** | Feed navigation | Sale cards not clickable | Click sale → navigate to detail page | Click has no effect or modal closes | Cannot navigate to published sales for iCal/Share testing |

---

## Console Errors

Only non-app error found: MetaMask wallet conflict (standard, not blocking).

No JavaScript application errors detected during navigation attempts.

---

## Recommendations

**Before next QA run:**

1. **Fix Export Data:** Verify that `/organizer/export` page exists or redirect the button to the correct URL. Check:
   - Is the page file present? (`packages/frontend/app/organizer/export/page.tsx`)
   - Is the button href correct?
   - If page is missing, either create it or hide/disable the button.

2. **Fix user12 Login:** Debug authentication for shopper account:
   - Check if user12@example.com exists in database with active status
   - Verify no account-level restrictions or role misconfigurations
   - Test login API directly (POST to `/api/auth/login`) with user12 credentials
   - Check auth service logs for rejection reason
   - Compare user2 (working) and user12 (failing) account records for differences

3. **Fix Feed Navigation:** Verify sale card click handlers:
   - Check if cards have proper href or onClick handlers
   - Test on multiple sales
   - Check for navigation routing errors in console

**After fixes, re-run:**
- BATCH A: A2–A5 (iCal, Share, À La Carte, Stripe Connect)
- BATCH B: All gamification features (requires user12 login)
- BATCH C: All shopper social features (requires user12 login)

---

## Session Notes

- Session logged in as user2 (organizer PRO) successfully
- Session attempted user12 (shopper) login multiple times — all failed
- 3+ attempts to click feed cards — no navigation
- Network requests show zero POST attempts for login, confirming client-side issue
- No application JS errors in console (MetaMask plugin error is environment-only)
- Page load times normal; no timeout or slow-load issues detected

---

**QA Session End:** Session terminated due to blocking infrastructure issues preventing feature testing. Core test coverage for BATCH B and C not possible until user12 login is restored.

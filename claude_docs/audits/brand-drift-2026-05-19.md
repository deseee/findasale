# Brand Drift Audit — 2026-05-19

**Run by:** weekly-brand-drift-detector scheduled task  
**Scope:** All .tsx files in packages/frontend/pages + components, skill SKILL.md files, D-001 through D-003 compliance spot-checks  
**Brand references:** claude_docs/brand/brand-voice-guide-2026-03-16.md, claude_docs/brand/DECISIONS.md  
**Prior audit:** 2026-05-12 (findings carried forward — prior fixes NOT applied)

---

## Summary

**2 P1 user-facing violations found.** The city `<title>` tag on one of the highest-traffic SEO pages lists only "Estate Sales & Yard Sales" — excluding auctions, flea markets, and consignment entirely. The findasale-marketing skill has a brand voice framing line that implicitly centers estate sales, which means all generated marketing copy inherits this bias.

Additionally, 5 P2 and 4 P3 findings below — mostly meta description omissions and one tooltip using estate sales as the universal benchmark.

All 4 code comment fixes from the **2026-05-12 audit remain unresolved** (marked as carry-forward below).

---

## Drift Findings

### New This Week

| File | Line | Issue | Severity | Decision |
|------|------|-------|----------|----------|
| `pages/city/[slug].tsx` | 60 | Page `<title>` = `"Estate Sales & Yard Sales in ${cityName}, ${cityState} \| FindA.Sale"` — auctions, flea markets, consignment absent. High-traffic SEO page title sets the brand impression before any copy is read. | **P1** | D-001 |
| `skills/findasale-marketing/SKILL.md` | 49 | Brand voice instruction reads: `"Think: a knowledgeable neighbor who happens to run estate sales."` — all marketing content generated by this skill inherits an estate-sale-centric lens. | **P1** | D-001 |
| `components/CityDirectorySection.tsx` | 63 | User-visible subtext: `"Estate sale companies, auction houses, and resale organizers operating in this area."` — garage sale hosts and flea market vendors not represented. Displayed on all city pages below the fold. | P2 | D-001 |
| `components/EfficiencyCoachingWidget.tsx` | 72 | Tooltip text: `"Industry average is 60–80% for estate sales."` — non-estate-sale organizers see a benchmark that doesn't apply to their event type. | P2 | D-001 |
| `pages/cities/index.tsx` | 47 | Meta description: `"Find estate sales, yard sales, auctions and more near you."` — flea markets not listed explicitly. | P2 | D-001 |
| `pages/neighborhoods/index.tsx` | 57 | Schema.org description: `"Find estate sales, yard sales, and auctions in neighborhoods near you."` — flea markets omitted. | P2 | D-001 |
| `pages/map.tsx` | 290–291 | Meta description: `"View estate sales, yard sales, auctions, and consignment sales on an interactive map."` — flea markets missing. (Map filter UI correctly includes flea-market type.) | P2 | D-001 |
| `pages/map.tsx` | 340 | When `saleCount === 0`, page shows `"0 sales near you"` with no empty state message, no CTA to adjust filters, expand radius, or check back. Dead end for new users. | P2 | D-003 |
| `pages/clearance/index.tsx` | 187 | Meta: `"Shop clearance items from recently ended estate sales, yard sales, and more."` — auctions and flea markets not called out. | P3 | D-001 |
| `components/EfficiencyCoachingWidget.tsx` | 97 | `text-green-500` bullet point without `dark:` variant — minor readability issue in dark mode. | P3 | D-002 |
| `components/ReturnToInventoryPanel.tsx` | 187 | CTA button uses `bg-sage-600 hover:bg-sage-700` with no `dark:` variant — button background doesn't adapt in dark mode. | P3 | D-002 |

### Carry-Forward from 2026-05-12 Audit (Unfixed)

| File | Line | Issue | Severity | Decision |
|------|------|-------|----------|----------|
| `pages/guide.tsx` | 3 | Code comment: `"Full walkthrough for estate sale organizers."` | P3 | D-001 |
| `pages/encyclopedia/index.tsx` | 1–4 | Code comments: `"Estate Sale Encyclopedia Index Page"` and `"Public encyclopedia of estate sale knowledge"` | P3 | D-001 |
| `pages/neighborhoods/[slug].tsx` | 3 | Code comment: `"SEO-optimised page for estate sales in a specific neighborhood."` | P3 | D-001 |
| `pages/organizers/[id].tsx` | 199 | OG meta description: `"Estate sales, auctions, and more from ${businessName}"` — partially addressed (meta description updated) but OG tag still leads with estate sales | P2 | D-001 |

---

## What Was Clean

**Homepage (index.tsx):** Hero copy, meta descriptions, schema.org all enumerate full sale type list. ✅

**Pricing page (pricing.tsx):** All tier descriptions are sale-type-agnostic. ✅

**About page (about.tsx):** Mission section inclusive of all types. ✅

**Organizer Guide — user-facing content:** Meta title `"Organizer Guide – FindA.Sale"` and meta description are type-agnostic. Only code comment drifts. ✅

**Encyclopedia — user-facing content:** Public title `"Resale Encyclopedia"` and description are inclusive. Only code comment drifts. ✅

**City pages — description tag:** `city/[slug].tsx` description tag correctly includes "estate sales, yard sales, auctions, flea markets and more." Only the `<title>` drifts. ✅

**findasale-skills (other):** findasale-dev, findasale-qa, findasale-records, findasale-architect — no estate-sale-only language. ✅

**Clearance empty state (D-003):** Has a proper empty state with icon, message, and implicit CTA. ✅

**Dark mode (D-002):** BulkStatusModal.tsx — clean. Layout.tsx component — clean. ✅

---

## Compliance Score

| Decision | Status | Notes |
|----------|--------|-------|
| D-001 All Sale Types | 🔴 P1 active | 2 P1 violations, 5 P2 violations, 4 P3 carry-forwards |
| D-002 Dark Mode | 🟡 Minor gaps | 2 P3 components with missing dark variants |
| D-003 Empty States | 🟡 1 gap | Map page missing empty state CTA |
| D-004 Mobile-First | ⬜ Not tested | Requires Chrome MCP |
| D-005 Multi-Endpoint | ⬜ Not tested | Requires Chrome MCP |

---

## Recommended Fixes

### Dispatch to findasale-dev (code changes)

**P1 — Fix immediately:**

1. `pages/city/[slug].tsx` line 60 — Change title from:
   ```
   `Estate Sales & Yard Sales in ${cityName}, ${cityState} | FindA.Sale`
   ```
   To:
   ```
   `Sales & Resale Events in ${cityName}, ${cityState} | FindA.Sale`
   ```
   (SEO note: keep "estate sales" in the description tag — it already includes full list.)

2. `pages/map.tsx` near line 340 — Add empty state block when `saleCount === 0`:
   ```tsx
   {saleCount === 0 && !isLoading && (
     <div className="text-center py-6 text-warm-600 dark:text-warm-400">
       <p className="font-medium">No sales found in this area.</p>
       <p className="text-sm mt-1">Try expanding your radius or removing filters.</p>
     </div>
   )}
   ```

**P2 — Batch fix:**

3. `components/CityDirectorySection.tsx` line 63 — Change:
   ```
   "Estate sale companies, auction houses, and resale organizers operating in this area."
   ```
   To:
   ```
   "Estate sale companies, garage sale hosts, auctioneers, flea market vendors, and other resale organizers in this area."
   ```

4. `components/EfficiencyCoachingWidget.tsx` line 72 — Change:
   ```
   "Industry average is 60–80% for estate sales."
   ```
   To:
   ```
   "Industry average is 60–80% for most resale events."
   ```

5. `pages/cities/index.tsx` line 47 — Change description to:
   ```
   'Find estate sales, yard sales, auctions, flea markets, and more near you. Browse sales in cities across the US on FindA.Sale.'
   ```

6. `pages/neighborhoods/index.tsx` line 57 — Add "flea markets" to schema.org description:
   ```
   'Find estate sales, yard sales, auctions, and flea markets in neighborhoods near you. Hyper-local sale discovery on FindA.Sale.'
   ```

7. `pages/map.tsx` meta lines 290–291 — Add "flea markets" to both meta description and schema.org description.

8. `pages/organizers/[id].tsx` line 199 — Change OG meta to:
   ```
   `Sales, auctions, and more from ${organizer.businessName}${locationSuffix}.`
   ```

**P3 — Low priority, batch with next routine maintenance:**

9. `components/EfficiencyCoachingWidget.tsx` line 97 — Add `dark:text-green-400` to the bullet span.

10. `components/ReturnToInventoryPanel.tsx` line 187 — Add `dark:bg-sage-500` to button className.

11–14. Code comments (carry-forward): guide.tsx line 3, encyclopedia/index.tsx lines 1–4, neighborhoods/[slug].tsx line 3 — update to inclusive language per 2026-05-12 recommendations.

### Dispatch to findasale-records (skill update)

**P1 — Fix the marketing skill brand voice framing:**

15. `skills/findasale-marketing/SKILL.md` line 49 — Change:
    ```
    Think: a knowledgeable neighbor who happens to run estate sales.
    ```
    To:
    ```
    Think: a knowledgeable neighbor who loves all kinds of community resale — estate sales, garage sales, auctions, flea markets.
    ```
    Then repackage the skill via skill-creator + mcp__cowork__present_files for Patrick to install.

---

*P1 items require findasale-dev dispatch before next marketing content generation.*  
*P2 items can batch as a single dev dispatch (no file conflicts).*  
*P3 items defer to next routine maintenance pass.*

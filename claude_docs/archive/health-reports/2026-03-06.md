# Health Report — 2026-03-06

## Summary

Overall codebase health is **GOOD** — no secrets exposed, CORS properly locked down, all write routes are authenticated, JWT has no bypass, and admin endpoints are protected with `requireAdmin`. The app is beta-ready from a security standpoint. Top 3 concerns to resolve before launch: (1) `alert()` dialogs in 4 places breaking UX polish, (2) 8 `<img>` tags missing `alt` text (accessibility/SEO), (3) one unhandled promise in `uploadController` that could silently swallow upload errors. The `buyingPoolController` nested `include: { user: true }` pattern is worth cleaning up but doesn't currently expose user data in API responses.

---

## Critical (blocker — fix before next deploy)

None found. ✓

---

## High (fix this week)

- **Admin `findMany` without pagination** — `packages/backend/src/controllers/adminController.ts:83,205,271,283,294` — Admin-only panel queries return unbounded result sets. At scale these could be slow or cause timeouts. Add `take: 100` to each. Low blast radius (admin role only) but worth fixing before beta traffic increases.

---

## Medium (fix this sprint)

- **`alert()` for UX feedback (4 places)** — Should use `showToast()` from ToastContext instead:
  - `packages/frontend/components/FeedbackWidget.tsx:14` — rating validation alert
  - `packages/frontend/components/FeedbackWidget.tsx:40` — submit failure alert
  - `packages/frontend/pages/map.tsx:153` — geolocation permission alert
  - `packages/frontend/pages/organizer/dashboard.tsx:106` — clone sale error alert

- **8 `<img>` tags missing `alt` text** — Accessibility + SEO issue:
  - `CartDrawer.tsx:163`, `ItemCard.tsx:82`, `ItemListWithBulkSelection.tsx:102`
  - `ItemPhotoManager.tsx:143`, `PhotoLightbox.tsx:90`, `RapidCapture.tsx:259,324`
  - `RecentlyViewed.tsx:40`

- **Unhandled promise** — `packages/backend/src/controllers/uploadController.ts:65` — `uploadToCloudinary(...).then(...)` with no `.catch()`. If Cloudinary upload fails here, the error is silently swallowed.

- **`tierController.ts` TODO admin check** — `packages/backend/src/controllers/tierController.ts:90` — Comment says "add admin check if needed". Verify if this endpoint is exposed publicly; if so, add `requireAdmin` middleware.

---

## Low (track, fix when relevant)

- **`.env.example` drift** — `packages/backend/.env.example` has `ANTHROPIC_MODEL`, `OLLAMA_URL`, `OLLAMA_VISION_MODEL` not present in `.env`. Conversely `.env` has `DIRECT_URL` and `LOCAL_DATABASE_URL` not documented in example. Ollama vars are deprecated (CB5 cleanup). Update example to match current `.env` shape.

- **`buyingPoolController` nested `include: { user: true }` pattern** — `buyingPoolController.ts:24,194,298` — Fetches full User record (including all fields) to extract email/name for notifications. Data is not returned to API clients, but the pattern is risky if a future developer adds `res.json(item)`. Switch to `select: { email: true, name: true }` inside the user include.

- **`tierController.ts` unconfirmed admin protection** — See Medium above. Confirmed as Low if endpoint is organizer-only (self-managed tier).

---

## Clean Checks

- **Secrets scan**: No hardcoded API keys, passwords, or tokens in source ✓
- **Sensitive console.log**: No password/token/secret logging found ✓
- **CORS**: Locked to `ALLOWED_ORIGINS` env var + Vercel preview URL regex ✓
- **JWT bypass**: No `ignoreExpiration` or `skipVerify` found ✓
- **Admin routes**: All protected with `authenticate` + `requireAdmin` middleware ✓
- **Write routes (messages, checklist, notifications, reservations)**: All use `router.use(authenticate)` ✓
- **SSR safety**: All `window`, `localStorage`, `navigator` calls are inside `useEffect` or event handlers ✓

---

## Self-Healing Reference

- `alert()` → toast pattern: well-established in this codebase, see any page that imports `useToast()`. Same fix every time.
- `img` alt text: add `alt={itemTitle}` or `alt=""` for decorative images.
- Unhandled promise: add `.catch(err => console.error('[upload]', err))`.

## Self-Healing Candidates

- **`alert()` → toast**: Appears 4 times across 3 files. Recommend adding to `self_healing_skills.md` as entry: "When alert() is found in frontend, replace with showToast() from ToastContext."

---

## Recommended Next Actions

1. Fix 4 `alert()` calls → `showToast()` (session 82 — ~15 min)
2. Add `alt` text to 8 `<img>` tags (session 82 — ~10 min)
3. Add `.catch()` to `uploadController.ts:65` (session 82 — 2 min)
4. Update `.env.example` to remove deprecated Ollama vars and add `DIRECT_URL` (session 82 — 5 min)
5. Add `take: 100` to admin `findMany` queries (session 82 — 10 min)

# CA4 + CA6 Audit — User Flow & Feature Polish
**Date:** 2026-03-05
**Sessions:** 69
**Status:** Fixes shipped

---

## CA4: User Flow Audit

### Shopper Journey
- **FIXED** `purchases.tsx` — added `isError` + retry button + "Browse Sales" CTA on empty state
- **FIXED** `index.tsx` — replaced `window.location.reload()` with React Query `refetch()`
- **FIXED** `search.tsx` — added `aria-label="Search sales and items"` (WCAG 2.1 AA)
- **FIXED** `items/[id].tsx` — improved error state with retry button instead of bare text
- **OPEN** `register.tsx` — some organizer fields missing `htmlFor` on labels (low priority, pre-beta)
- **OPEN** `organizer/create-sale.tsx` — no client-side date start/end validation (medium)
- **OPEN** `organizer/add-items/[saleId].tsx` — no manual single-item add form (medium-high, CA6 Phase 2)

### Organizer Journey
- **OPEN** `organizer/payouts.tsx` + `organizer/bounties.tsx` — gray color system instead of warm (low)
- **OPEN** `creator/dashboard.tsx` — analytics/settings tabs show placeholder text (low)

### Creator/Affiliate Journey
- **FIXED** `referral-dashboard.tsx` — copy button now shows "Copied!" feedback + empty referrals state
- **OPEN** `organizer/bounties.tsx` — status badge colors don't match warm palette (low)

### A11y Summary
| Issue | File | Status |
|-------|------|--------|
| Search input no aria-label | search.tsx | ✅ Fixed |
| Login social buttons | login.tsx | Open (low) |
| Register htmlFor gaps | register.tsx | Open (medium) |
| Confirm password aria-required | register.tsx | Open (low) |

---

## CA6: Feature Polish

### Photo Upload UX
- **FIXED** `ItemPhotoManager.tsx` — added 5MB client-side file size validation with per-file error message
- **FIXED** `ItemPhotoManager.tsx` — surfaces backend error message instead of generic "Upload failed"
- **OPEN** Upload progress bar (currently button-label only) — post-beta
- **OPEN** Mobile touch-drag reorder (currently arrow buttons only) — post-beta

### Push Notifications
- **FIXED** `organizer/settings.tsx` notifications tab — added push notification enable/disable toggle
- **FIXED** `OnboardingModal.tsx` step 3 — improved notification description to mention settings control
- **OPEN** Profile page (`profile.tsx`) — no push toggle for shoppers (should add matching toggle)
- **OPEN** iOS < 16.4 fallback messaging — silent unsupported, no UX hint

### Onboarding Walkthrough
- **FIXED** `OnboardingModal.tsx` — step 3 body text updated to set clearer expectations
- **OPEN** Organizer onboarding — no post-registration walkthrough for ORGANIZER role
- **OPEN** Mobile layout testing on iPhone SE screen sizes

### Empty States & Error Handling
- **FIXED** `items/[id].tsx` — error state now shows retry button
- **FIXED** `purchases.tsx` — error state + empty state CTA added
- **OPEN** `profile.tsx` query error handling (medium)
- **OPEN** `categories/[category].tsx`, `city/[city].tsx`, `neighborhoods/*` — no error state (medium)
- **OPEN** No global React Error Boundary component (post-beta)

---

## Files Changed This Batch
- `packages/frontend/public/favicon.ico` — NEW (16/32/48px multi-size from logo-icon-512.png)
- `packages/frontend/pages/_document.tsx` — added `favicon.ico` shortcut link
- `packages/frontend/pages/index.tsx` — refetch() instead of window.location.reload()
- `packages/frontend/pages/shopper/purchases.tsx` — isError handling + CTA on empty
- `packages/frontend/pages/search.tsx` — aria-label on search input
- `packages/frontend/pages/items/[id].tsx` — improved error state with retry
- `packages/frontend/components/ItemPhotoManager.tsx` — 5MB validation + surfaced errors
- `packages/frontend/components/OnboardingModal.tsx` — improved step 3 description
- `packages/frontend/pages/organizer/settings.tsx` — push notification toggle
- `packages/frontend/pages/referral-dashboard.tsx` — copy feedback + empty referrals state

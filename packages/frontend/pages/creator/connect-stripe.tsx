/**
 * DEPRECATED — Marked for deletion (S294).
 * This was a legacy redirect to /organizer/settings?tab=payments.
 * File should be removed from git in next push.
 */
export default function DeprecatedConnectStripe() {
  return null;
}
